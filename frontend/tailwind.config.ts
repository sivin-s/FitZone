// Tailwind v4: Configuration lives in index.css via @theme / @plugin.
// This file only exists for DaisyUI v5 theme selection (passed via CSS @plugin).
// Do NOT add content[], theme.extend, or plugins[] here — those are v3 patterns.
export default {};